# Loop 0017 - 남은 개발 항목

## 아직 남은 항목

1. `guard_retry_timeline` 비-seed 표본 장기 누적 관측
- 대상: 이번 루프 재관측으로 `30 -> 35`까지 증가 확인했으므로, 임계치 드리프트(오탐/미탐) 판단을 위해 추가 누적 구간(예: 50+) 관측을 수행

2. P7 운영 전환 최종 수용 리포트 확정
- 대상: 이번 루프에서 생성한 fail-closed/count-lock 묶음 근거(JSON)와 임계치 기준선/회귀 로그를 단일 승인 패키지로 정리
